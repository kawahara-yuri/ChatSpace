.chat-side__groups-list
  .chat-side__group
    - current_user.groups.each do |group|
      = link_to group_message_path(group) do
        .chat-side__group-name
          = group.name
        .chat-side__group-message
          メッセージはまだありません。
