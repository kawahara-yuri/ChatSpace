.chat-side
  .chat-side__user
    .chat-side__user-content
      .chat-side__user-name
        = current_user.name
      = link_to new_group_path do
        = fa_icon 'pencil-square-o', class: "chat-side__user--new-group"
      = link_to edit_user_path(current_user) do
        = fa_icon 'cog', class: 'chat-side__user--edit-user'
  .chat-side__groups
    .chat-side__groups-list
      - current_user.groups.each do |group|
        .chat-side__group
          = link_to group_messages_path(group), style: "text-decoration: none; color: #fff;" do
            .chat-side__group-name
              = group.name
            .chat-side__group-message
              メッセージはまだありません。
